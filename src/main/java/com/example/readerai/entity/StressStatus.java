package com.example.readerai.entity;

public enum StressStatus {
    // Аудіо ще не почало оброблятися шаром перевірки наголосу.
    PENDING,
    // Python-сервіс виконує forced alignment + інференс моделі (асинхронно,
    // незалежно від того, чи користувач ще на екрані результатів).
    PROCESSING,
    DONE,
    FAILED,
    // Еталонний текст замалий/непридатний для перевірки наголосу (немає
    // жодного діагностичного слова) — не помилка, просто нема що показати.
    NOT_APPLICABLE
}
